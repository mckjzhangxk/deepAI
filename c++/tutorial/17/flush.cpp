#include <iostream>
int main() {
    using std::cin;
    using std::cout;
    using std::endl;
    using std::flush;
    cout << "input sth:" << flush;
    // cout << (flush);
    int a;
    cin >> a;
}